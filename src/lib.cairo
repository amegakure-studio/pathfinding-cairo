mod pathfinder;

mod algorithms {
    mod jps;
}

mod data_structures {
    mod map;
    mod min_heap;
    mod tile;
    mod tiles_info;
}

mod numbers {
    mod i64;
    mod integer_trait;
}

mod utils {
    mod constants;
    mod heuristics;
    mod movement;
}

#[cfg(test)]
mod tests {
    // mod test_map;
    mod test_jps;
// mod test_heap;
}
